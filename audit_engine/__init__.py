"""Brand AI-Readiness Audit engine.

A deterministic-first pipeline that crawls a website, builds a shared
SiteSnapshot, runs specialized analyzers to produce evidence-backed
Findings, and emits a single schema-validated JSON audit report.

See README.md at the repository root for the full architecture writeup.
"""

__version__ = "1.0.0"
